# Type-check why.benchkit for browser JS (exercises `#if (js && !nodejs)` branches).
# Usage: haxe check-js.hxml
-cp src
-lib travix
-lib why-unit
-js dump/check-js.js
--no-output
why.benchkit.Runner
why.benchkit.Config
why.benchkit.Measure
